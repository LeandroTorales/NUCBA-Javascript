const arrayHelados = ["chocolate", "vainilla", "tramontana", "granizado", "dulce de leche"];
/* const heladosModificar = (sabor) => {
  arrayHelados.unshift(arrayHelados.pop());
  arrayHelados.push(sabor);
  console.log(arrayHelados);
};
heladosModificar("chocotorta"); */

/* const heladosModificar2 = () => {
  arrayHelados.push(arrayHelados.shift());
  console.log(arrayHelados);
};
heladosModificar2();
 */

/* const invertirArray = () => {
  console.log(arrayHelados.reverse().join(" - "));
};

invertirArray();
 */
const arrayHelados2 = ["limon", "naranja", "uva"];

/* const modificarArrays = () => {
  let arrayNuevo = arrayHelados
    .slice(arrayHelados.length - 2)
    .concat(arrayHelados2.slice(0, 2))
    .join(" - ");
  console.log(arrayNuevo);
};
modificarArrays();
 */
/* 
const gustosAbreviados = (gustos) => {
  let nuevoArray = [];
  for (i = 0; i < gustos.length; i++) {
    nuevoArray.push(gustos[i].slice(0, 3).toUpperCase());
  }
  console.log(nuevoArray);
};
gustosAbreviados(arrayHelados);
 */

let stringEjercicio = "              .levin us ed sojel yum atse isseM y odnum led rodaguj rojem le se odlanoR                   ";
/* const invertirString = () => {
  let newString = stringEjercicio.trim().split("").reverse().join("").replace("Messi", "Ronaldo").replace("Ronaldo", "Messi");

  console.log(newString);
};
invertirString(); */
